"""Web-plugin (.gan) module for theDAW.

Imports VST Foundry exports into portable .gan packages (ZIP + manifest + web
assets), serves a plugin's UI to an iframe, and packages bundled plugins such as
"The Owl" as VST-style sidecar files that can be revealed and shared.
"""
