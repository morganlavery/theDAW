"""Sheet-music / score import module.

Parses notated scores (MusicXML, ABC, Humdrum kern, MIDI) into piano-roll note
batches via music21 (a core dependency). See ``parser.py`` for the mapping onto
the piano roll's 16th-note step grid.
"""
