/**
 * Shared select field for the properties panels. This is the existing
 * `CustomSelect` re-exported under the properties/fields barrel so both panels
 * consume the same select control (and the shared option sets in `../options`).
 * Output and behaviour are identical to using `CustomSelect` directly.
 */
export { default } from "../../CustomSelect";
