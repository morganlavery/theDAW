# Local tools
codegraph/
.codegraph/

# Secrets & user data
data/api_key_pools.json
# TOUR third-party keys (ORS, OpenChargeMap), saved filter preset, provider cache
data/tour_keys.json
data/tour_filters.json
data/tour_cache/
# Underfit upstream-update check cache (written by backend/modules/underfit/updater.py)
data/underfit_update.json
# App update check cache (written by backend/modules/updates/router.py)
data/updates_check.json
# Quest deploy config: adb path override, last APK, default package (user-local)
data/quest.json
# Quest deploy APK downloads pulled from theDAW-XR releases (~150 MB each)
data/quest/
data/generations/
data/settings.json
data/recent_projects.json
data/local_checkpoints.json
data/library.db
data/library.db-*
# Suno cloud-gen key/jobs + local session change logs (never committed)
data/suno_api_key.json
data/suno_jobs.json
# Installed/built .gan plugins + their extracted runtime (regenerated on demand)
data/plugins/
# Machine-specific VST3 scan cache (regenerated on scan)
backend/modules/vst/vst3_scan_cache.json
# Captured VST3 native-editor state (per-plugin presets, machine-specific)
data/vst_presets/
CHANGES-LOGS/
backend/rag_index/
archive/
# Generated git-history visualisation (scripts/gen_git_graph.py) — a local artifact
docs/git-history.html
*.png
!docs/UI/**/*.png
!docs/guides/**/*.png
!docs/readme/**/*.png
# Bundled .gan plugin artwork (The Owl) — a required runtime asset, not a capture still
!frontend/public/owl/*.png
# Bundled .gan plugin artwork (Ares) — the VST Foundry export's background.png beside
# its project.json; required to package ares.gan (without it the surface has no background)
!backend/modules/plugin/assets/**/*.png
# The desktop app icon — electron-builder's buildResources auto-discovers it and
# converts to .ico/.icns. It MUST be tracked: a fresh CI checkout otherwise builds
# installers with the default Electron icon (local builds mask this because the
# file exists untracked on the dev machine).
!electron-ui/resources/icon.png

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
# Python build artifact dirs — anchored to repo root so `frontend/src/lib/`
# (our shared frontend module directory) isn't accidentally ignored.
/lib/
/lib64/
/parts/
/sdist/
/var/
# Ignore build-artifact wheels, but TRACK the vendored prebuilt aubio wheels:
# aubio is source-only on PyPI, so these bundled per-platform wheels (pinned in
# pyproject [tool.uv.sources], built by .github/workflows/build-aubio-wheels.yml)
# are what let a compiler-less Windows / macOS / Linux box install aubio.
/wheels/*
!/wheels/aubio-0.4.9-cp310-cp310-win_amd64.whl
!/wheels/aubio-0.4.9-cp310-cp310-macosx_11_0_arm64.whl
!/wheels/aubio-0.4.9-cp310-cp310-manylinux2014_x86_64.manylinux_2_17_x86_64.manylinux_2_28_x86_64.whl
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#pdm.lock
#   pdm stores project-wide configurations in .pdm.toml, but it is recommended to not include it
#   in version control.
#   https://pdm.fming.dev/#use-with-ide
.pdm.toml

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/

# uv package cache kept on the repo's drive so wheels hardlink into .venv
# (set by theDAW.bat / install/setup.ps1). Runtime-only, can be multi-GB.
.uv-cache/
env.bak/
venv.bak/

# Isolated runtime venv + filtered reqs for the faster-whisper transcription sidecar
backend/modules/vocal/transcription/.whisper_venv/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

*.ckpt
*.wav
wandb/*
*.out

.gradio
.DS_Store
*.safetensors
.claude
# Custom ignores
repomix-output.md
*.bak-*
.playwright-mcp/
.serena/
local_models.txt
grafting_day/frontend-graft-manifest.json
frontend.bak*/

# TypeScript incremental build cache — regenerated by tsc -b on every build,
# noisy in diffs, and machine-specific timestamps.
*.tsbuildinfo

# Node dependency folders — generated by npm/pnpm/yarn installs. Keep lockfiles
# committed, but never vendor package trees from frontend or sidecars.
node_modules/

# Local backup snapshots — never commit
_RECOVERY_BACKUP/


# Reference PDFs kept locally, not version-controlled (large binaries)
docs/*.pdf

# Local archive of superseded/scratch files (see archive/README.md) — not tracked
/archive/
/research/
.vite/
.orb_attachments/

# Generated exports (VJ renders, transcodes, PNG-sequence zips, etc.) — NEVER
# commit exports of any kind. The VJ exporter writes here by default.
/exports/

# Session changelogs — local dev notes, not version-controlled
/docs/changes/

# Session/branch reports — local working notes, not version-controlled
/docs/reports/

# Loose capture stills & scratch media at repo root (mirror the *.png policy so
# ad-hoc .jpeg screenshots like shot01_*.jpeg never get committed). Anchored to
# root so frontend image assets are unaffected.
/*.jpeg
/*.jpg
/.tmp-shots/

# VS Code workspace files are per-user / machine-specific — never commit.
*.code-workspace

# Showcase assets are local-only (demo video, screenshots, scripts, raw media).
# Kept on disk but not shipped in everyone's clone.
/showcase/

# Root-level scratch artifacts (debug dumps, one-off harnesses, reference
# prototypes, personal media) stay local. Anchored to root so real assets
# elsewhere are unaffected.
/_*.json
/_*.py
/*.mp3
/dna-polyphonic-splice*.html
/temp-tools/doc-audit-*/
/temp-tools/doc-audit-manifest.json

# Local audit artifacts: vulnerability details stay off the remote.
/CODEBASE_AUDIT_*.md
/audit-reports/

# Internal working-notes and status docs — local only, not user documentation.
# Kept out of the tracked tree and the installer.
/docs/plans/
/docs/audits/
/docs/changes/
/docs/in-flight-work.md
/docs/WHERE-WE-STAND.md
/docs/TASMO-VST-DAW-IMPORT-ROADMAP.md
/docs/DESIGN_PRINCIPLES.md
/docs/DESIGN_WIREFRAME.svg

# Electron desktop shell — build artifacts + runtime logs (deps via node_modules/ above)
electron-ui/out/
electron-ui/release/
# Bundled runtime binaries fetched at build time (uv.exe, ffmpeg.exe) — large,
# downloaded from official sources by scripts/fetch-runtime-tools.mjs.
electron-ui/resources/tools/
# Staged VJ production build (gantasmo/VJ-9000 dist), regenerated at package
# time by scripts/fetch-vj-build.mjs.
electron-ui/resources/vj-dist/
# Staged VST Foundry production bundle (dist/ + prod node_modules), regenerated
# at package time by scripts/fetch-foundry-build.mjs.
electron-ui/resources/foundry-app/
logs/

# Editing/backup cruft (e.g. vendored apps' *.bak-<date> files)
*.bak
*.bak-*
deprecated/

# Local model weights (multi-GB safetensors + configs) - never commit
models/

# Underfit is vendored as a git-subrepo (dada-bots/underfit + local mods): its
# CODE is tracked by theDAW; only its heavy runtime dirs are excluded here (also
# covered by underfit/.gitignore). state/ is multi-GB — never commit it.
/underfit/state/
/underfit/.venv/
/underfit/.gradio/
/underfit/**/node_modules/
/underfit/**/__pycache__/

# Stems sidecar (integration-package/backend) runtime artifacts.
# Only the sidecar's code is committed; its venv, logs, results, and the
# LARSNET weights are created or placed at runtime.
integration-package/backend/.sidecar_venv/
integration-package/backend/.sidecar_logs/
integration-package/backend/.sidecar_venv_requirements.txt
integration-package/backend/backend_port.txt
integration-package/backend/logs/
integration-package/backend/results/
integration-package/backend/larsnet/pretrained_larsnet_models/
integration-package/backend/larsnet/pretrained_larsnet_models.zip
